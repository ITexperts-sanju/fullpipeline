apiVersion: v1
kind: Service
metadata:
  name: myapp
  namespace: default
spec:
  selector:
    app: myapp
  type: NodePort            # <-- change here
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080       # <-- desired port
